repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
  - name: documents-api
    labels:
      app: documents-api
    wait: true
    chart: bitnami/aspnet-core
    version: 4.4.7
    values:
      - values.yaml
      - image:
          tag: "{{ .StateValues.image.tag }}"
      - ingress:
          hostname: "{{ .StateValues.ingress.hostname }}"
      - extraSecretEnvVars:
          ConnectionStrings__DefaultConnection: "{{ .StateValues.extraSecretEnvVars.ConnectionStrings__DefaultConnection }}"
          AuthenticationOptions__PublicSigningKey: "{{ .StateValues.extraSecretEnvVars.AuthenticationOptions__PublicSigningKey }}"
          InnerCircleServiceUrls__EmployeesApiRootUrl: "{{ .StateValues.extraSecretEnvVars.InnerCircleServiceUrls__EmployeesApiRootUrl }}"
          InnerCircleServiceUrls__EmailSenderApiRootUrl: "{{ .StateValues.extraSecretEnvVars.InnerCircleServiceUrls__EmailSenderApiRootUrl }}"
          